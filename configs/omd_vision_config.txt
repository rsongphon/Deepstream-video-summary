[property]
gpu-id=0
model-engine-file=/workspace/models/d_model/onnx/omd_vision_with_preprocessing.engine

# Model batch size (should match your export)
batch-size=4

# FP16 mode (2=FP16, 1=INT8, 0=FP32)
network-mode=2

# Classifier type (1 = classifier, use as needed)
network-type=100

# Unique ID for nvinfer instance
gie-unique-id=1

# Input tensor is RGB (0 = RGB, 1 = BGR, 2 = GRAY)
model-color-format=0

# Input tensor dims (C;H;W), here NCHW: 3;640;640
infer-dims=3;640;640

# 1 = primary, 2 = secondary (leave as 1 for primary branch)
process-mode=1

# Disable aspect ratio crop/pad (0 = stretch to fit)
maintain-aspect-ratio=0

# Attach output tensors as user-meta (0)
output-tensor-meta=1

# *** DISABLE all DeepStream normalization! ***
net-scale-factor=1.0
offsets=0;0;0

# Inference every Nth frame (as you wish)
interval=25